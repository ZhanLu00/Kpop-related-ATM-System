package ATM.Managers;

public class RequestManager {
    /* Request manager
     * Attributes
     * * type - new user
     *        - new account
     * * username
     * * account type
     * * status - declined
     *          - accepted
     *          - pending
     * Methods
     * * setAlert(type, username, accountType)
     * * getAlert(type, username, accountType)
     * * getAllUserAlert()
     * * requestExist(username) -> boolean
     * * getStatus(type, username, accountType)
     */
}
